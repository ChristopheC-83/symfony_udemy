{% block header %}

	<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
		<a class="navbar-brand" href="#">Navbar</a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item active">
					<a href="{{ path('app_main') }}" class="btn btn-light btn-sm m-3 p-3">Retour Accueil</a>
				</li>
				<li class="nav-item">
					<a href="{{ path('app_contact') }}" class="btn btn-light btn-sm m-3 p-3">Contact</a>
				</li>
				<li class="nav-item">
					<a href="{{ path('app_tableaux') }}" class="btn btn-light btn-sm m-3 p-3">Tableaux</a>
				</li>

			</ul>
		</div>
	</nav>

{% endblock %}
